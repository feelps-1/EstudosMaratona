#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int parts, actual_parts;
    int cuts;

    cin >> parts;

    char last_cut;

    while(actual_parts != parts){

    }

    return 0;
}